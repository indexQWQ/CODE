#include <stdio.h>
int main ()
{
regain:// 循环语句
    printf("Hello, world!\n");
    goto regain;// 跳转语句
    return 0;
}

//go to语句的作用是使程序从当前位置跳转到指定的位置，并继续执行。
//goto语句的格式是：goto 标签名；
//标签名是用户自定义的标识符，用于标识程序执行的位置。
//goto语句的作用是使程序从当前位置跳转到指定的位置，并继续执行。
//标签名是用户自定义的标识符，用于标识程序执行的位置。

// 注意：goto语句的使用要慎重，因为它会导致程序的执行流程发生变化，容易造成程序的混乱。
// 一般情况下，goto语句只用于程序的复杂分支结构，而不用于一般的循环结构。
// 另外，goto语句的使用也要注意程序的可读性，不能滥用。
// 因此，在实际编程中，应尽量避免使用goto语句。

// go to 真正适合的场景是：
// 1. 复杂的分支结构，需要跳转到不同的位置执行不同的代码。
// 2. 循环结构，需要跳转到循环的开头，重复执行循环体。

// for()
// {
//     for()
//     {
//         for()
//         {
//             if(disater)
//             {
//                 go to error;
//             }
//         }
//     }
// }
// error:
//     printf("disater happen");
