#include <stdio.h>
#include <math.h>

// double sqrt(double x) 
// {
//     return sqrt(x);
// }

int main() 
{
    double x = 24.0;
    double result = sqrt(x);
    printf("The square root of %f is %f\n", x, result);
    return 0;
}